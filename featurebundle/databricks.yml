# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "featurebundle"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405606023758734.14.azuredatabricks.net

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://adb-7405606023758734.14.azuredatabricks.net
      # We always use /Users/somayehkeyvan1368@yahoo.com for all resources to make sure we only have a single copy.
      root_path: /Users/somayehkeyvan1368@yahoo.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as somayehkeyvan1368@yahoo.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: somayehkeyvan1368@yahoo.com
